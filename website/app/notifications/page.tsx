'use client';import {useEffect,useState} from 'react';import Shell from '../../components/DashboardShell';import {api} from '../../lib/api';export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api('/notifications').then(setD).catch(()=>{})},[]);return <Shell><h1>Notifications</h1>{d.map(x=><div className="card" key={x.id}><b>{x.title}</b><p>{x.body}</p><small>{new Date(x.created_at).toLocaleString()}</small></div>)}</Shell>}
