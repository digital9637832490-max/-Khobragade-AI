'use client'; import {useState} from 'react';import Shell from '../../components/DashboardShell';import {api} from '../../lib/api';
export default function Page(){const[text,setText]=useState('');const[msg,setMsg]=useState('');async function go(){try{const d=await api('/ai/voiceover',{method:'POST',body:JSON.stringify({text})});setMsg(`Job ${d.id} created`)}catch(e:any){setMsg(e.message)}}return <Shell><h1>Voice-over</h1><div className="card"><p>Default cost: 10 coins. Admin can change it.</p><textarea placeholder="Voice-over text" value={text} onChange={e=>setText(e.target.value)}/><button className="button" onClick={go}>Confirm & Generate</button><p>{msg}</p></div></Shell>}
