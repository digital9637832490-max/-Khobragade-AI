'use client'; import {useState} from 'react'; import {api} from '../../lib/api';
export default function Login(){const[e,setE]=useState('');const[p,setP]=useState('');const[msg,setMsg]=useState('');
async function go(){try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email:e,password:p})});localStorage.setItem('token',d.token);location.href='/dashboard'}catch(x:any){setMsg(x.message)}}
return <main className="main"><div className="card" style={{maxWidth:460,margin:'40px auto'}}><h1>Login</h1><input placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><button className="button" onClick={go}>Login</button><p>{msg}</p><a href="/forgot-password">Forgot password?</a></div></main>}
