# Android app

Flutter scaffold using the same backend API as the website.

Run:
`flutter run --dart-define=API_BASE_URL=http://10.0.2.2:4000/api`

Production:
- set HTTPS API URL
- add secure token storage package
- add image/file picker
- add push notification SDK
- configure Android package ID, signing, app icons and Play Store metadata
