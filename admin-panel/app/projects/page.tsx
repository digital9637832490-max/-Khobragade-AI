'use client';import {useEffect,useState} from 'react';import Shell from '../../components/AdminShell';import {api} from '../../lib/api';export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{api('/admin/jobs').then(setD).catch(()=>{})},[]);return <Shell><h1>Project / Job Management</h1>{d.map(x=><div className="card" key={x.id}><b>{x.tool_key} · {x.status}</b><p>{x.user_name} · {x.email}</p><p>Coins: {x.coin_cost} · Attempts: {x.attempts}</p></div>)}</Shell>}
